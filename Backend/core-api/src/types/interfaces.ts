export interface IAccessDecodedPayload {
    _id:string,
    fullname:string
    isActive:boolean 
}

export interface IRefreshDecodedPayload {
    _id:string,
    fullname:string,
    email:string
    isActive:boolean
}